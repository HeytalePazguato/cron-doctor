name: pages

# Build and deploy the GitHub Pages site from /docs.
#
# Triggers on changes under /docs (or this workflow itself) on develop or
# main. develop pushes deploy a preview-style site continuously; main pushes
# deploy on stable releases. Only one Pages deploy runs at a time.
on:
  push:
    branches: [develop, main]
    paths:
      - "docs/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/jekyll-build-pages@v1
        with:
          source: ./docs
          destination: ./_site
      - uses: actions/upload-pages-artifact@v5

  deploy:
    needs: build
    # Only deploy from main; develop pushes still run the build job to
    # validate the Jekyll site, but the github-pages environment is
    # protected to main only.
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
