## Summary

Brief description of what this PR does and why.

## Changes

-

## Test plan

- [ ] `go vet ./...` passes
- [ ] `go test -race ./...` passes
- [ ] Manually exercised on a representative crontab fixture
- [ ] CHANGELOG `[Unreleased]` updated (if user-visible change)

## Screenshots / sample output

If this changes user-facing output (text report, JSON, calendar), paste a
before/after sample.

## Checklist

- [ ] Targets `develop` (not `main`)
- [ ] Branch name is descriptive (`feat/...`, `fix/...`, `docs/...`)
- [ ] No `[beta]` / `[rc]` keywords in commit messages (those are reserved
      for release branches)
